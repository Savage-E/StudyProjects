namespace Paired_peaks.Utils
{
    public class RowsCalculator
    {
        public string CalculateRows(string data )
        {
           int count =  data.Split('\n').Length -1;
           return "";
        }
    }
}